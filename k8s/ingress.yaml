apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: sk000-myfirst-ingress
  namespace: skala-practice
spec:
  ingressClassName: public-nginx
  rules:
  - host: sk000-ingress.skala25a.project.skala-ai.com
    http:
      paths:
      - backend:
          service:
            name: sk000-myfirst-api-server
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 'sk000-ingress.skala25a.project.skala-ai.com'
    secretName: sk000-ingress-project-tls-cert
